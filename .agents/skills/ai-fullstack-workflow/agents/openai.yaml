interface:
  display_name: "Ai Fullstack Workflow"
  short_description: "Repo-specific patterns and workflows for ai-fullstack-workflow"
  default_prompt: "Use the ai-fullstack-workflow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true