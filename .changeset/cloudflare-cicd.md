---
"@repo/web": minor
"@repo/api": minor
---

Move web to Cloudflare Workers (OpenNext), add multi-environment deploys
(preview per-PR, dev, production) on custom domains via GitHub Actions, and
switch the branch model to dev-default with manual production promotion.
